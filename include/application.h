#ifndef BINARY_RELATION_ANALYZER_APPLICATION_H
#define BINARY_RELATION_ANALYZER_APPLICATION_H

/** Runs the command-line application and returns a process exit code. */
int run_application(int argc, char *argv[]);

#endif
